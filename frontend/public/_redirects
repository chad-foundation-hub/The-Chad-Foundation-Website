# 1. Force all traffic from the .netlify.app URL to the primary domain
https://chad-foundation.netlify.app/* https://chad-foundation.org/:splat  301!

# 2. API Redirects (MUST come before the React Catch-All)
/api/* /.netlify/functions/:splat  200

# 3. Handle the React Single Page App (SPA) routing (The "Fall Back")
/* /index.html  200