STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
FRONTEND_URL=
REACT_APP_STRIPE_PUBLISHABLE_KEY=
# Comma-separated list of allowed origins (e.g., "https://example.com,https://app.example.com"); use "*" to allow all origins (not recommended for production).
ALLOWED_ORIGINS=

# Database configuration
NETLIFY_DATABASE_URL=
# Example: postgresql://username:password@host:port/database?sslmode=require
